import React from 'react'

function Categories() {

    const Adventure = new Map(['site3','site2'])
    const Relax = new Map(['site4'])
    const holiday = new Map(['site1'])

    return (
        <div>
            
        </div>
    )
}

export default Categories
